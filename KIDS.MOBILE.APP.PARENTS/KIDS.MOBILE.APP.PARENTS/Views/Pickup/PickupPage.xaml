<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KIDS.MOBILE.APP.PARENTS.Views.Pickup.PickupPage"
             xmlns:loadingView="clr-namespace:KIDS.MOBILE.APP.PARENTS.Controls.LoadingView;assembly=KIDS.MOBILE.APP.PARENTS"
             xmlns:markupExtension="clr-namespace:KIDS.MOBILE.APP.PARENTS.MarkupExtensions;assembly=KIDS.MOBILE.APP.PARENTS"
             xmlns:calendar="clr-namespace:Syncfusion.SfCalendar.XForms;assembly=Syncfusion.SfCalendar.XForms">

    <ContentPage.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="MonthTemplate">
                <Grid BackgroundColor="White" 
                      HorizontalOptions="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Label Text="{Binding Date.Day}" 
                            HorizontalTextAlignment="Center" 
                            VerticalTextAlignment="Center" 
                            VerticalOptions="FillAndExpand" 
                            HorizontalOptions="FillAndExpand" 
                            TextColor="Black" 
                            FontAttributes="None" 
                            FontSize="10" />
                    <Image Source="{Binding Image}" 
                            WidthRequest="20" 
                            HeightRequest="20" 
                            HorizontalOptions="Center" 
                            VerticalOptions="Center"
                           IsVisible="{Binding IsVisible}"
                           Grid.Row="1"/>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid HorizontalOptions="FillAndExpand"
          VerticalOptions="FillAndExpand"
          BackgroundColor="#f0eded">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Header-->
        <StackLayout HorizontalOptions="FillAndExpand"
                     VerticalOptions="FillAndExpand"
                     Orientation="Horizontal"
                     Padding="5,10,5,10"
                     BackgroundColor="White"
                     Grid.Row="0">
            <Image Source="back.png"
                   HorizontalOptions="Start">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BackCommand}"/>
                </Image.GestureRecognizers>
            </Image>
            <Label HorizontalOptions="CenterAndExpand"
                   Text="{markupExtension:Translate 00043}"
                   TextColor="Black"
                   FontAttributes="Bold"
                   FontSize="24">
            </Label>
        </StackLayout>

        <loadingView:LoadingView
            x:Name="loadingView"
                Grid.Row="1"
                IsVisible="{Binding IsLoading}"
                HorizontalOptions="CenterAndExpand"
                VerticalOptions="CenterAndExpand"/>

        <calendar:SfCalendar x:Name="calendar"
                             OnMonthCellLoaded="Calendar_OnMonthCellLoaded"
                             MonthChanging="Calendar_MonthChanged"
                             Margin="10"
                             HorizontalOptions="FillAndExpand"
                             VerticalOptions="FillAndExpand"
                             BackgroundColor="White"
                             ViewMode="MonthView"
                             Grid.Row="1">
            <calendar:SfCalendar.MonthViewSettings>
                <calendar:MonthViewSettings CellTemplate="{StaticResource MonthTemplate}" />
            </calendar:SfCalendar.MonthViewSettings>
        </calendar:SfCalendar>

        <!--Information-->
        <!--<Frame Margin="10,5,10,5"
               BackgroundColor="White"
               CornerRadius="10"
               Padding="0"
               Grid.Row="1">
            <Grid HorizontalOptions="FillAndExpand"
                  VerticalOptions="FillAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="150"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackLayout HorizontalOptions="FillAndExpand"
                             VerticalOptions="CenterAndExpand"
                             Orientation="Horizontal"
                             Grid.Row="0"
                             Grid.ColumnSpan="2">
                    <Image Source="leave.png"
                           HorizontalOptions="Start"
                           VerticalOptions="CenterAndExpand"
                           Margin="10,0,20,0"/>
                    <Label Text="{Binding TimePeriod}"
                           LineBreakMode="WordWrap"
                           MaxLines="2"
                           Margin="10,0,20,0"
                           VerticalOptions="CenterAndExpand"
                           HorizontalOptions="FillAndExpand"/>
                </StackLayout>

                <Frame Margin="10,0,5,0"
                       HorizontalOptions="FillAndExpand"
                       VerticalOptions="FillAndExpand"
                       BackgroundColor="#f0eded"
                       CornerRadius="10"
                       Grid.Row="1"
                       Grid.Column="0">
                    <StackLayout HorizontalOptions="FillAndExpand"
                                 VerticalOptions="CenterAndExpand"
                                 Orientation="Vertical">
                        <Image Source="done.png"
                               HorizontalOptions="CenterAndExpand"
                               VerticalOptions="CenterAndExpand"/>
                        <Label Text="Ve dung gio"
                               HorizontalTextAlignment="Center"
                               VerticalTextAlignment="Center"
                               FontSize="16"
                               TextColor="Black"/>
                        <Label Text="{Binding OnTime}"
                               HorizontalTextAlignment="Center"
                               VerticalTextAlignment="Center"
                               FontSize="16"
                               TextColor="Black"
                               FontAttributes="Bold"/>
                    </StackLayout>
                </Frame>

                <Frame Margin="5,0,10,0"
                       HorizontalOptions="FillAndExpand"
                       VerticalOptions="FillAndExpand"
                       BackgroundColor="#f0eded"
                       CornerRadius="10"
                       Grid.Row="1"
                       Grid.Column="1">
                    <StackLayout HorizontalOptions="FillAndExpand"
                                 VerticalOptions="CenterAndExpand"
                                 Orientation="Vertical">
                        <Image Source="error.png"
                               HorizontalOptions="CenterAndExpand"
                               VerticalOptions="CenterAndExpand"/>
                        <Label Text="Ve muon"
                               HorizontalTextAlignment="Center"
                               VerticalTextAlignment="Center"
                               FontSize="16"
                               TextColor="Black"/>
                        <Label Text="{Binding Late}"
                               HorizontalTextAlignment="Center"
                               VerticalTextAlignment="Center"
                               FontSize="16"
                               TextColor="Black"
                               FontAttributes="Bold"/>
                    </StackLayout>
                </Frame>
            </Grid>
        </Frame>-->

        <!--Create new request-->
        <!--<StackLayout HorizontalOptions="FillAndExpand"
                             VerticalOptions="CenterAndExpand"
                             Orientation="Horizontal"
                             Grid.Row="2"
                             Margin="10,5,10,5">
                    <Image Source="add.png"
                           HorizontalOptions="Start"
                           VerticalOptions="CenterAndExpand"
                           Margin="10,0,20,0"/>
                    <Label Text="Ban ban ron, can nguoi don be ve?"
                           LineBreakMode="WordWrap"
                           MaxLines="2"
                           Margin="10,0,20,0"
                           VerticalOptions="CenterAndExpand"
                           HorizontalOptions="FillAndExpand"/>
                </StackLayout>-->

        <!--Request List-->
        <!--<ScrollView HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand"
                    Grid.Row="3"
                    Margin="10">
            <syncfusion:SfListView
                ItemsSource="{Binding MessagesList}"
                HorizontalOptions="CenterAndExpand"
                VerticalOptions="CenterAndExpand"
                Margin="10"
                FlowDirection="LeftToRight"
                ItemSize="180"
                Orientation="Vertical">

                <syncfusion:SfListView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="10"
                               IsClippedToBounds="True"
                               Margin="0,5,0,5"
                               HorizontalOptions="FillAndExpand"
                               VerticalOptions="FillAndExpand">
                            <template:MessageTemplate
                                Padding="10,0,10,0"
                                ColumnSpacing="0"
                                BackgroundColor="White" />
                        </Frame>
                    </DataTemplate>
                </syncfusion:SfListView.ItemTemplate>
            </syncfusion:SfListView>
        </ScrollView>-->
    </Grid>
</ContentPage>
